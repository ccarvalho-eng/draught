defmodule Draught do
  @moduledoc """
  Provider-neutral infrastructure for coding agents on the BEAM.

  Draught keeps provider, tool, runtime, persistence, and client concerns behind
  explicit contracts. The public API will grow as those contracts are introduced.
  """
end
